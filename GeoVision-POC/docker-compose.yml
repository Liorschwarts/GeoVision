services:
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env

  # python-service:
  #   image: your-python-image
  #   ports:
  #     - "8000:8000"
